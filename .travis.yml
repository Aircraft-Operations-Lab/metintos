version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"
  commands:
  - pip install dask==2021.4.1
  - pip install casadi==3.5.5
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script:
  - pytest
